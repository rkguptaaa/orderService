version: 0.2

phases:
  build:
    commands:
      - echo Entered the build phase...
      - mvn clean
  post_build:
    commands:
      - echo Build completed successfully
      - ls -l
artifacts:
  artifacts:
  files:
    - target/order-service.jar
  discard-paths: yes
